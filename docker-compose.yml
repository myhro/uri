---
version: "3.0"
services:
  app:
    build: .
    image: myhro/uri
    volumes:
      - .:/app
    working_dir: /app
  db:
    image: postgres:9.6-alpine
    environment:
      - POSTGRES_DB=uri
    volumes:
      - .:/app
    working_dir: /app
